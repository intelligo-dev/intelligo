/**
 * Stub for the real ProseMirror text editor — `lib/chat-canvas-config.tsx`
 * types a lazy import against this path, but nothing in the film ever
 * opens an artifact tool result, so the editor itself (and its
 * ProseMirror dependency) is never needed.
 */
export default function TextEditorStub(_props: Record<string, unknown>): null {
  return null;
}
