---
import Page from "@/layouts/Page.astro";
import { RegistryExplorer } from "@/components/registry-explorer";
import { InstallCommand } from "@/components/install-command";
import { REGISTRY_COUNT } from "@/lib/registry-items";
import { SITE } from "@/lib/site";

const words = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten", "eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen", "twenty", "twenty-one", "twenty-two", "twenty-three", "twenty-four", "twenty-five", "twenty-six", "twenty-seven", "twenty-eight", "twenty-nine", "thirty"];
const count = words[REGISTRY_COUNT] ?? String(REGISTRY_COUNT);
const Count = count.charAt(0).toUpperCase() + count.slice(1);

const RULES = [
  ["Installed components are used as-is.", "What a deployment varies arrives through config files the items ship for you to edit: navigation, banners, onboarding steps, credit bundles, chat identity, conversation starters, tool renderers."],
  ["Copy is translation, not code.", "Every item reads its strings from a per-item next-intl namespace and ships messages/en/<item>.json. Adding a language means adding message files — never editing a component."],
  ["Business rules live behind the pages.", "An installed action parses, calls a typed service, maps the error, revalidates. The invariants are in the packages, tested against a real database."],
];
---
<Page
  path="/pages"
  tag="pages · the registry"
  title={`${Count} page families,<br />shipped as your own source.`}
  lede="Installed with the shadcn CLI from the registry this site hosts at /r. Pages, components, loading, empty and error states, and thin actions — rendered by your own shadcn primitives. No runtime UI package, nothing to fork later."
  description={`${REGISTRY_COUNT} Intelligo page families, installed as consumer-owned source through a shadcn-compatible registry.`}
>
  <section class="xline">
    <div class="px-6 py-10 md:px-10 md:py-14">
      <div class="grid gap-px border border-line bg-line md:grid-cols-3">
        {RULES.map(([h, b]) => (
          <div class="bg-paper p-4">
            <div class="text-[0.98rem] font-semibold text-ink">{h}</div>
            <p class="mt-1 text-[0.86rem] text-ink-dim">{b}</p>
          </div>
        ))}
      </div>
      <div class="mt-6">
        <div class="tag mb-2">install any item</div>
        <InstallCommand client:visible cmd={`pnpm exec shadcn add ${SITE.registryBase}/<item>.json`} />
      </div>
    </div>
  </section>

  <section id="explorer" class="xline">
    <div class="px-6 py-10 md:px-10 md:py-14">
      <RegistryExplorer client:load />
      <p class="mono mt-4 text-[0.72rem] text-ink-faint">
        Items marked "the real component" render the files shadcn add installs; the rest are sketches.
        <a href={`${SITE.adrs}/0010-registry-i18n-standard.md`} class="ml-1 hover:text-ink">ADR-0010 · registry items are i18n-native and installed unmodified →</a>
      </p>
    </div>
  </section>
</Page>
