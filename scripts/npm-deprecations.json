{
  "$comment": "npm names the framework no longer publishes, each with where the code went. A maintainer applies these with `node scripts/npm-maintain.mjs` (trusted publishing cannot deprecate); an entry already on the registry is skipped, so the list only ever grows.",
  "deprecations": [
    {
      "name": "@intelligo-dev/money",
      "message": "Folded into @intelligo-dev/core/money (ADR-0011)."
    },
    {
      "name": "@intelligo-dev/http",
      "message": "Folded into @intelligo-dev/core/request-context (the contract) and @intelligo-dev/next (the Next.js binding) (ADR-0011)."
    },
    {
      "name": "@intelligo-dev/billing-core",
      "message": "Folded into @intelligo-dev/billing/{plans,plan-registry,payment,quota-types} (ADR-0011)."
    },
    {
      "name": "@intelligo-dev/ui",
      "message": "Replaced by the registry design system (ADR-0013): shadcn base-nova plus https://intelligo.dev/r/intelligo.json."
    }
  ]
}
