{
  "$comment": "The single allowlist behind ADR-0006. The public-export audit and the extraction script both read this file so they cannot drift; a package missing from both lists fails the audit.",
  "public": [
    "admin",
    "audit",
    "auth",
    "billing",
    "billing-core",
    "cli",
    "core",
    "executions",
    "jobs",
    "mastra",
    "ui"
  ],
  "$comment_deprecated": "ADR-0008 resolved ADR-0006's undecided trio: agents and ai dissolve, chat shrinks to its renderer registry. Until their exports reach their named destinations they stay in this repository, are NOT extracted, and no public package may depend on them.",
  "deprecated": ["agents", "ai", "chat"],
  "apps": ["app"],
  "rootFiles": [
    "LICENSE",
    "SECURITY.md",
    "CONTRIBUTING.md",
    "TRADEMARK.md",
    ".prettierrc",
    ".gitignore",
    "eslint.config.mjs",
    "turbo.json",
    "tsconfig.base.json",
    "vitest.config.ts",
    "pnpm-workspace.yaml",
    "docker-compose.yml",
    "CHANGELOG.md",
    "README.md"
  ],
  "directories": [
    "config",
    "docs/adr",
    "registry",
    "tests/architecture",
    "tests/stubs"
  ],
  "$comment_overlay": "Files written on top of the extracted tree, verbatim. They are the public repository's own versions of files whose incubation copies name private workspaces (CI, AGENTS.md, .env.example, CODEOWNERS, the ADR index). Nothing under this directory is itself extracted.",
  "overlay": "scripts/public-overlay",
  "$comment_env": "Environment variable names that belong to private providers; stripped from turbo.json's globalEnv in the extracted tree.",
  "privateEnv": [
    "QPAY_USERNAME",
    "QPAY_PASSWORD",
    "SOCIALPAY_USERNAME",
    "SOCIALPAY_PASSWORD",
    "TAVILY_API_KEY",
    "BLOB_READ_WRITE_TOKEN"
  ]
}
